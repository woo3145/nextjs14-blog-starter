---
title: 구현된 MDX 정리
description: 현재 사용가능한 MDX
slug: 'mdx-syntax'
---

## 기능 정리

<Tabs defaultValue="step">

<TabsList>
  <TabsTrigger value="step">Step 기능</TabsTrigger>
  <TabsTrigger value="codeblock-wrapper">CodeBlock 접기</TabsTrigger>
</TabsList>

<TabsContent value="step" >
<br/>
## Preview

<Steps>

<Step>1단계</Step>

```bash
yarn add woo3145
```

<Step>2단계</Step>

```ts title="hi.ts" {5-18}
console.log('Hi!');
```

<Step>3단계</Step>

app/hello.ts

```ts title="hi.ts" {5 - 18}
console.log('Hello!');
```

</Steps>

## Usage

```tsx
<Steps>
  <Step>1 단계</Step>
  ...contents
  <Step>2 단계</Step>
  ...contents
  <Step>3 단계</Step>
  ...contents
</Steps>
```

</TabsContent>

<TabsContent value="codeblock-wrapper">

<br />
## Preview

@libs/motions.ts

<CodeBlockWrapper
  expandButtonTitle="Expand"
  className={'my-6 overflow-hidden rounded-md'}
>

```ts title="motion.ts"
import { Variants } from 'framer-motion';

export const slideInFromTop = (delay?: number): Variants => {
  return {
    hidden: { y: -100, opacity: 0 },
    visible: {
      y: 0,
      opacity: 1,
      transition: {
        delay: delay,
        duration: 0.5,
      },
    },
  };
};

export const slideInFromLeft = (delay?: number): Variants => {
  return {
    hidden: { x: -100, opacity: 0 },
    visible: {
      x: 0,
      opacity: 1,
      transition: {
        delay: delay,
        duration: 0.5,
      },
    },
  };
};

export const slideInFromRight = (delay?: number): Variants => {
  return {
    hidden: { x: +100, opacity: 0 },
    visible:ㅋ {
      x: 0,
      opacity: 1,
      transition: {
        delay: delay,
        duration: 0.5,
      },
    },
  };
};
```

</CodeBlockWrapper>

## Usage

```tsx
<CodeBlockWrapper
  expandButtonTitle="Expand"
  className={'my-6 overflow-hidden rounded-md'}
>
  Contents or CodeBlock
</CodeBlockWrapper>
```

</TabsContent>

</Tabs>
